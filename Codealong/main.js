function maxOfThree(num1, num2, num3){
  if(num1 > num2 && num1 > num3){
    return num1;
  }else if (num2 < num && num2 > num1){
    return num2;
  }else{
    return num3;
  }
}

document.write(maxOfThree(30, 30 ,8));


function isVowel(letter){
  if (letter != "a" && letter !="e" && )
}
